(() => {
  const protocolVersion = 1;
  const limits = { sourceBytes: 100000, propsBytes: 32000, messageBytes: 150000, domBytes: 12000, consoleEntries: 50, networkEntries: 20, idBytes: 256 };
  const params = new URLSearchParams(location.search), runId = params.get("runId") || "runtime", revision = Number(params.get("revision") || 0), nonce = params.get("nonce") || "";
  const rootHost = document.getElementById("root"); let disposed = false, activeRequestId = null, messageCount = 0, renderCount = 0, root = null;
  const networkAttempts = [], parentAccess = [];
  const post = (message) => { if (disposed || ++messageCount > 100) return; try { if (new TextEncoder().encode(JSON.stringify(message)).byteLength <= limits.messageBytes) parent.postMessage(message, "*"); } catch {} };
  const record = (bucket, value) => { if (bucket.length < limits.networkEntries) bucket.push(value); };
  const blocked = (kind) => { record(networkAttempts, kind); return Promise.reject(new Error("Network access is disabled in the React sandbox.")); };
  try { Object.defineProperty(window, "fetch", { configurable: false, writable: false, value: () => blocked("fetch") }); } catch {}
  for (const key of ["XMLHttpRequest", "WebSocket", "EventSource"]) try { Object.defineProperty(window, key, { configurable: false, writable: false, value: function () { record(networkAttempts, key); throw new Error("Network access is disabled in the React sandbox."); } }); } catch {}
  try { Object.defineProperty(navigator, "sendBeacon", { configurable: false, writable: false, value: () => { record(networkAttempts, "sendBeacon"); return false; } }); } catch {}
  const identity = { protocolVersion, runId, revision, nonce };
  const result = (requestId, runtimeError = null) => { post({ type: "runtime:result", ...identity, requestId, dom: runtimeError ? "" : (rootHost?.innerHTML || "").slice(0, limits.domBytes), runtimeError, console: window.__forgeLogs || [], renderCount, networkAttempts, parentAccess }); activeRequestId = null; };
  const execute = (message) => { if (disposed || activeRequestId || message.nonce !== nonce || message.runId !== runId || message.revision !== revision) return; activeRequestId = message.requestId; window.__forgeLogs = []; try { const factory = new Function("props", message.source + "\n;return typeof App !== 'undefined' ? App : (typeof Component !== 'undefined' ? Component : null);"); const Component = factory(message.props); if (typeof Component !== "function") throw new Error("React lesson must define an App or Component function."); if (!rootHost) throw new Error("React runtime root is unavailable."); root?.unmount(); root = ReactDOM.createRoot(rootHost); root.render(React.createElement(Component, { ...message.props })); renderCount += 1; setTimeout(() => result(message.requestId), 0); } catch (error) { result(message.requestId, error instanceof Error ? error.message : String(error)); } };
  const onMessage = (event) => { const message = event.data; if (!message || message.protocolVersion !== protocolVersion || message.nonce !== nonce || message.runId !== runId || message.revision !== revision) return; if (message.type === "runtime:dispose") { disposed = true; root?.unmount(); removeEventListener("message", onMessage); } else if (message.type === "runtime:execute" && typeof message.source === "string" && message.source.length <= limits.sourceBytes && message.props && typeof message.props === "object") execute(message); else if (message.type === "runtime:reset") { root?.unmount(); root = null; renderCount = 0; window.__forgeLogs = []; post({ type: "runtime:ready", ...identity }); } else if (message.type === "runtime:interact" && typeof message.target === "string") { const target = document.querySelector(`[data-interaction="${CSS.escape(message.target)}"]`); if (target instanceof HTMLElement) target.click(); setTimeout(() => result(message.requestId), 0); } };
  addEventListener("message", onMessage); post({ type: "runtime:ready", ...identity });
})();
